# Output Schema Checks

Audit timestamp: 2026-05-01T14:22Z

Rule: new audit outputs must declare required columns and whether the generated file passed a basic schema check. All audit CSV fields are exported as source text/string values unless a source file already encodes a numeric-looking value.

| Output | Required columns checked | Rows | Schema status | Evidence |
|---|---|---:|---|---|
| `out/scanner_latest.csv` | `asin`, `supplier_sku`, `candidate_id`, `cost`, `buy_box_price`, `pf` | 51 | CONFIRMED | no missing required columns, no duplicate headers |
| `out/db_snapshot.csv` | `seller_sku`, `asin` | 608 | BROKEN | duplicate header `last_updated_A003` inherited from Product DB source |
| `out/link_check.csv` | `ASIN`, `Exists_in_DB`, `New_or_Existing` | 50 | CONFIRMED | no missing required columns, no duplicate headers |
| `out/pricing_output.csv` | `sku`, `current_cycle_decision`, `execution_write_status`, `write_attempted_flag`, `write_applied_flag` | 89 | CONFIRMED | no missing required columns, no duplicate headers |
| `project_control/SCRIPT_INVENTORY.csv` | `path`, `extension`, `top_area`, `flow_group`, `inferred_role`, `has_python_main_guard`, `mtime_utc`, `size_bytes` | 675 | CONFIRMED | source inventory generated from root entrypoints, `scripts/`, `tests/`, and scheduler config |
| `out/cycle_alerts/due_check_register_status.csv` | `check_id`, `owner_flow`, `title`, `register_status`, `due_state`, `due_utc`, `trigger`, `artifact_path`, `success_condition`, `failure_action`, `last_checked_utc`, `last_result`, `observed_utc`, `alert_status`, `notes` | 19 | CONFIRMED | generated by `scripts/tools/due_check_register.py`; latest run status `warn`, 9 due, 10 warn, 0 fail |
| `out/cycle_alerts/f_price_list_post_restart_mot.csv` | `check`, `status`, `value`, `notes`, `observed_utc`, `source_path` | 6 | CONFIRMED | generated by `scripts/tools/f_price_list_post_restart_mot.py`; focused tests confirm fresh OK, orphan drain FAIL, stale supervisor FAIL, login request without child FAIL, and login child event OK |
| `out/cycle_alerts/morning_mot_system_check.csv` | `observed_utc`, `phase`, `system`, `check`, `status`, `value`, `classification`, `repair_action`, `notes`, `source_path` | 6 | CONFIRMED | generated by `scripts/tools/morning_mot_system.py`; live proof wrote A/E/B/H/F/System rows with 0 fail and 2 warn |
| `out/cycle_alerts/morning_mot_system_check.json` | `status`, `observed_utc`, `phase`, `row_count`, `fail_rows`, `warn_rows`, `output_path`, `repair_path`, `repair_enabled`, `allow_a_repair` | 1 | CONFIRMED | generated with the CSV summary and points to the active repair-action report |
| `out/cycle_alerts/morning_mot_repair_actions.json` | `observed_utc`, `phase`, `repair_enabled`, `allow_a_repair`, `actions` | 1 | CONFIRMED | generated by repair mode; live proof had no runtime fail rows, so actions was empty |
| `out/cycle_alerts/cycle_failure_events.csv` | `event_id`, `timestamp_utc`, `cycle`, `run_id`, `final_state`, `cause_code`, `cause_detail`, `step_name`, `stage`, `rc`, `verification_status`, `manifest_path`, `health_path`, `source_path`, `recovery_action` | 0 | CONFIRMED | schema defined in `scripts/core/cycle_failure_events.py`; focused test `tests/test_cycle_failure_events.py` confirms header validation and idempotent upsert; A015 check `shared_cycle_failure_ledger_schema` watches the header contract; live file is created on first terminal failure event |

## Failed Schema Details

### `out/db_snapshot.csv`

- Failure: duplicate header `last_updated_A003`.
- Root source: `out/product_db_preview.csv` and SQLite table `sys_product_db_preview` contain the same duplicate source column name.
- Required fix location: Product DB source schema or the export logic that creates `out/product_db_preview.csv`.
- Downstream-only renaming would mask the root problem, so this audit left the duplicate visible.
