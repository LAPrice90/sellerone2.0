{
  "id": "F040_build_feeder_candidate_approval_queue",
  "display_name": "F040 Feeder Candidate Approval Queue",
  "flow": "F",
  "purpose": "Register feeder candidate approval queue proof so the manager can inspect recommendations and pending-review rows without approving anything.",
  "owner_entrypoint": "scripts/flows/F/F040_build_feeder_candidate_approval_queue.py",
  "worker_entrypoint": "scripts/flows/F/F040_build_feeder_candidate_approval_queue.py",
  "health_sources": [
    {
      "name": "feeder_approval_health",
      "path": "out/systems/F/live/feeder_approval_health.csv",
      "required": false,
      "max_age_minutes": 43200
    }
  ],
  "status_sources": [
    {
      "name": "feeder_shared_pass_logic_live",
      "path": "out/systems/F/live/feeder_shared_pass_logic_live.csv",
      "required": false,
      "max_age_minutes": 43200
    },
    {
      "name": "feeder_approval_decisions_log",
      "path": "out/systems/F/history/feeder_approval_decisions_log.csv",
      "required": false,
      "max_age_minutes": 43200
    }
  ],
  "outputs": [
    {
      "name": "feeder_candidate_recommendations_live",
      "path": "out/systems/F/live/feeder_candidate_recommendations_live.csv",
      "required": false,
      "max_age_minutes": 43200
    },
    {
      "name": "feeder_approval_queue_live",
      "path": "out/systems/F/live/feeder_approval_queue_live.csv",
      "required": false,
      "max_age_minutes": 43200
    },
    {
      "name": "feeder_approval_decisions_log",
      "path": "out/systems/F/history/feeder_approval_decisions_log.csv",
      "required": false,
      "max_age_minutes": 43200
    }
  ],
  "freshness_rules": [
    "feeder_approval_health_warn_after_43200_minutes",
    "approval_queue_rows_are_read_only_manager_evidence"
  ],
  "needs_user_signals": [
    "candidate_approval_required",
    "manual_review_pressure_high",
    "approval_source_contract_failed"
  ],
  "safe_actions": [
    "read_status",
    "read_report",
    "read_snapshot"
  ],
  "forbidden_actions": [
    "edit_worker_logic",
    "run_worker_cycle",
    "write_google_sheets",
    "change_queue_state",
    "change_pricing",
    "delete_outputs",
    "restart_worker",
    "approve_f061_handoff",
    "write_live_scanner_inbox",
    "run_f061",
    "approve_candidate_rows",
    "reject_candidate_rows",
    "write_purchase_handoff",
    "align_local_db"
  ],
  "manager_notes": "Manager label only. Approval queue proof may be read, but candidate approval, rejection, purchase handoff, and live scanner actions remain protected."
}
